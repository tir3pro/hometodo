import React from 'react';

import './styles.css';

const NotFound = () => {
    return (
        <div className='hometodo__not-found'>
            <h2 className='hometodo__not-found__title'>Page not found! Use navigation above to leave this page.</h2>
        </div>
    );
};

export default NotFound;